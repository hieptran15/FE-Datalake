export enum InputType {
 TEXT =  'TEXT' ,
  TEXTAREA = 'TEXTAREA',
  NUMBER = 'NUMBER',
  DATE = 'DATE',
  SELECT = 'SELECT',
  RADIO = 'RADIO',
  CHECKBOX = 'CHECKBOX',
}

export const MAX_SAFE_INTEGER = Math.pow(2, 31) - 1;
